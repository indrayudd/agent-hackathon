# Cleanup 10

- [x] Investigate modern web-native interactive plotting options for statistical reports and capture a practical recommendation.
- [x] Add a backend story plot contract that bridges notebook outputs into normalized report figure metadata.
- [x] Make the story frontend consume normalized figure metadata instead of inferring plot behavior from notebook cells.
- [x] Standardize figure sizing and scaling so report visuals render consistently across sections.
- [x] Keep takeaways as the only card-like summary surface while flattening the analysis narrative into a report layout.
- [x] Re-run frontend type checks after the rollout.
- [ ] Live-verify the report view in the browser after the current Playwright browser lock clears.
